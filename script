local mainBar = path.to.MainBar
local barLeft = mainBar.AbsolutePosition.X
local barWidth = mainBar.AbsoluteSize.X

-- Calculate the center 4% of the bar (the dark green strip)
local centerMinX = barLeft + (barWidth * 0.48)
local centerMaxX = barLeft + (barWidth * 0.52)

if indicatorCenterX >= centerMinX and indicatorCenterX <= centerMaxX then
    -- Trigger perfect hit action
end
