local VIM, UIS, Cam = game:GetService("VirtualInputManager"), game:GetService("UserInputService"), workspace.CurrentCamera; local interval, active = 3.3, false; print("Script Loaded! Press G to toggle."); UIS.InputBegan:Connect(function(input, gProc) if gProc or input.KeyCode ~= Enum.KeyCode.G then return end active = not active; print("Auto-M1:", active); if active then task.spawn(function() while active do task.wait(interval) if not active then break end local size = Cam.ViewportSize; VIM:SendMouseButtonEvent(math.floor(size.X/2), math.floor(size.Y/2), 0, true, game, 1); task.wait(0.05); VIM:SendMouseButtonEvent(math.floor(size.X/2), math.floor(size.Y/2), 0, false, game, 1); print("Clicked center!") end end) end end)
